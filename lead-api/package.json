{
  "name": "orfab-simulation-leads",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute orfab-simulation-leads --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute orfab-simulation-leads --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
